<Slider
    x:Class="Dtimu.Controls.MySlider"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dtimu.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400" >
    <Slider.Template>
        <ControlTemplate TargetType="Slider">

            <Grid x:Name="Root"
              Background="Transparent">

                <!-- 横向 Slider 模板 -->
                <Grid x:Name="HorizontalTemplate"
                  MinHeight="44">

                    <!-- 背景轨道 -->
                    <Grid x:ConnectionId='1' Background="Transparent" Height="18" 
                          VerticalAlignment="Center" 
                                                                
                                                                         >
                        <Rectangle x:Name="HorizontalTrackRect"
                           Height="4"
                           Fill="#DDD"
                           />
                    </Grid>

                    <!-- 已播放部分 -->
                    <Rectangle x:Name="HorizontalDecreaseRect"
                           Height="4"
                           Fill="#ED5A65"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Left"/>
                    <Canvas>
                        <Thumb x:ConnectionId='2' x:Name="HorizontalThumb" Canvas.Top="12"                                
                       Width="18"
                       Height="18"
                       VerticalAlignment="Center"
                       Background="Transparent"
                                                                        
                                                            >

                            <Thumb.Template>
                                <ControlTemplate TargetType="Thumb">
                                    <Ellipse Width="18"
                     Height="18"
                     Fill="#ED5A65"/>
                                </ControlTemplate>
                            </Thumb.Template>

                        </Thumb>
                    </Canvas>

                </Grid>

            </Grid>

        </ControlTemplate>
    </Slider.Template>


</Slider>

